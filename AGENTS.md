# Stockcast 개발 지침

이 파일은 저장소 전체에 적용된다. 하위 디렉터리에 더 구체적인 `AGENTS.md`가 있으면 해당 지침을 함께 따른다.

## 프로젝트 개요

- Python 3.11 이상과 `src` 레이아웃을 사용하는 국내주식 KIS Open API 프로젝트다.
- 패키지 코드는 `src/stockcast/`, 테스트는 `tests/`에 둔다.
- CLI 진입점은 `stockcast.cli:main`, 웹 진입점은 `stockcast.web:main`이다.
- 주요 책임은 다음 경계를 유지한다.
  - `config.py`: 환경 파일 읽기, 설정 모델, 설정 검증
  - `kis.py`: KIS 인증, 호출 제한, 시세·잔고·주문 API
  - `strategy.py`: 부작용 없는 매매 신호 계산
  - `autotrade.py`: 모의투자 자동매매 상태, 후보 선별, 실행 루프
  - `web.py`: Flask 라우팅, 세션, CSRF, API 응답 조립
  - `templates/`, `static/`: 서버 렌더링 HTML과 브라우저 UI

## 코드 스타일

- 기존 코드 스타일과 파일별 책임을 유지한다. 관련 없는 대규모 재작성이나 새 프레임워크 도입을 피한다.
- Python 파일은 `from __future__ import annotations`, 타입 힌트, 표준 라이브러리 우선 원칙을 따른다.
- 설정과 요청처럼 값 객체인 모델은 가능하면 `@dataclass(frozen=True)`로 만든다.
- 외부 API 응답은 경계에서 검증하고, 사용자에게는 한국어로 이해 가능한 오류를 제공한다.
- KIS 경로, TR ID, 필드명은 추측하지 말고 한국투자증권 공식 Open Trading API 자료와 대조한다.
- KIS REST 호출은 `KISClient`를 통해 수행한다. 호출 간격 제어와 `EGW00201` 재시도 정책을 우회하지 않는다.
- 주문 요청은 결과가 불확실할 때 자동 재전송하지 않는다. 중복 주문 가능성을 우선 차단한다.
- HTML에 외부 문자열을 삽입할 때 텍스트로 렌더링하고, 불가피한 경우 명시적으로 이스케이프한다.
- 기존 CSRF 검사, 세션 쿠키 설정, localhost 바인딩 및 보안 응답 헤더를 약화하지 않는다.

## 거래 안전 원칙

- 새 자동화 기능은 기본적으로 모의투자 전용으로 구현하고 테스트한다.
- 실전 주문 기능을 변경할 때는 기존의 환경·설정·명시적 확인 장치를 제거하거나 완화하지 않는다.
- 주문 허용 종목, 최대 주문금액, 보유 종목 수, 손절 및 중지 장치를 주문 직전에도 검증한다.
- 자동매매 변경 시 중복 주문, 미체결, 잔고 반영 지연, API 호출 제한, 장외·휴장 상황을 고려한다.
- 수익을 보장하는 표현을 코드, UI 또는 문서에 추가하지 않는다.

## 보안 정보

- API 키, 앱 시크릿, 접근 토큰, 계좌번호, 세션 값 또는 실제 주문 응답을 커밋하지 않는다.
- `.env`, `.env.paper`, `.env.live`, `.stockcast/` 등 로컬 비밀·상태 파일은 Git 추적 대상에서 제외한다.
- 예제 파일에는 실제 값 대신 빈 값이나 명백한 플레이스홀더만 사용한다.
- 테스트에서는 가짜 키와 마스킹된 계좌번호를 사용하고 네트워크 호출은 모킹한다.
- 로그, 예외, 테스트 출력, 스크린샷에 인증정보가 나타나지 않도록 한다.
- 사용자의 기존 환경 파일을 덮어쓰거나 비밀값을 출력하지 않는다. 설정을 갱신할 때는 관련 키만 보존적으로 수정한다.

## 테스트와 검증

- 기능 수정에는 같은 변경을 검증하는 단위 또는 웹 테스트를 추가하거나 갱신한다.
- 테스트는 `unittest`와 `unittest.mock`의 현재 패턴을 유지하고 실제 KIS 서버나 실계좌에 의존하지 않는다.
- 변경 후 최소한 다음 전체 테스트를 실행한다.

```powershell
python -m unittest discover -s tests -v
```

- Python 변경 시 필요에 따라 다음 컴파일 검사도 실행한다.

```powershell
python -m compileall -q src
```

- 브라우저 JavaScript 변경 시 문법을 검사한다.

```powershell
node --check src\stockcast\static\app.js
```

- 테스트 실패를 숨기거나 기존 안전 테스트를 삭제해 통과시키지 않는다. 실행하지 못한 검증은 최종 보고에 이유와 함께 명시한다.

## 문서와 의존성

- 설치법, 실행법, 환경 파일, CLI, 웹 UI, 자동매매 동작, 안전 제한 또는 지원 범위가 바뀌면 같은 변경에서 `README.md`도 업데이트한다.
- README의 설명이 실제 코드와 다르면 코드 변경 범위와 관련된 오래된 내용을 함께 바로잡는다.
- 런타임 의존성을 추가하거나 버전 범위를 바꾸면 `pyproject.toml`에 반영하고 필요성을 설명한다.
- 새 환경 변수는 안전한 기본값과 검증을 제공하고, 비밀값 없이 예제 및 README에 문서화한다.

## 변경 범위와 완료 기준

- 사용자 변경과 관련 없는 파일은 수정하지 않는다.
- 생성물, 캐시, `__pycache__`, 로컬 환경 파일을 결과물로 커밋하지 않는다.
- 완료 전 코드·테스트·README·`pyproject.toml` 사이의 이름, 기본값, 실행 명령이 일치하는지 확인한다.
- 최종 응답에는 변경 결과, 실행한 테스트와 남아 있는 위험 또는 실제 KIS 환경에서 필요한 검증을 간결하게 알린다.
